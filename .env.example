# Copy this file to .env and adjust values
# Note: VITE_ICE_JSON must be a SINGLE-LINE JSON array (no line breaks)
VITE_ICE_JSON=[{"urls":["stun:turn.example.video:3478"]},{"urls":["turn:turn.example.video:3478?transport=udp","turn:turn.example.video:3478?transport=tcp","turns:turn.example.video:5349?transport=tcp"],"username":"user","credential":"secret"}]
# Optional: force TURN-only routing in restrictive networks ("relay" or "all")
VITE_ICE_TRANSPORT_POLICY=all
# Optional: override API/WS base (usually leave empty; same-origin is used in prod)
# VITE_API_BASE=https://example.video
# VITE_WS_BASE=wss://example.video
# Backend uses this to generate absolute room links (QR)
# Example for LAN tests from another device:
# PUBLIC_BASE_URL=http://192.168.1.50:5173
